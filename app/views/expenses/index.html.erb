<h3>Expense Records</h3>

<%= link_to "Add expenses", new_expense_path, class: "btn btn-primary" %>
<%= link_to "Year-End Report" ,{:controller => "expenses", :action => "year_end_report", :year => Time.now.year}, class: "btn btn-info pull-right" %>


<div class="table-responsive incomes">
	<table id="myTable" class="table table-condensed table-hover table-striped table-bordered users">
		<thead>
		<tr>
			<th>Date</th>
			<th>Expense</th>
			<th>Memo</th>
			<th>Amount</th>
			<th width="125px"></th>
		</tr>
		</thead>
		<tbody>
		<% @expenses.each_with_index do |expense, i| %>
		<tr>
			<td><%= expense.date  %></td>
			<td><%= link_to expense.expense_type, expense_path(expense.id)  %></td>
			<td><%= expense.description %></td>		
			<td><%= expense.amount %></td>
			<td >
			<span class="pull-right">
				<%= link_to "Edit", edit_expense_path(expense.id), class: "btn btn-primary btn-small" %>
				|
				<%= link_to "Delete", expense_path(expense.id), method: :delete, data: { confirm: 'Are you sure?' }, class: "btn btn-warning btn-danger btn-small" %>
			</span>
		</td>
		</tr>

		</tbody>
		<%end%>
		<tr>
			<td colspan="7">
				<%= link_to "New Entry", new_expense_path, class: "pull-right btn btn-primary btn-small" %>	
				</a>
			</td>
		</tr>
	</table>

</div>

