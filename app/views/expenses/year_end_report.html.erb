<h3>Expense Records for Fiscal Year XX</h3>

<div class="table-responsive incomes">
	<table id="myTable" class="table table-condensed table-hover table-striped table-bordered users">
		<thead>
		<tr>
			<th>Date</th>
			<th>Expense</th>
			<th>Memo</th>
			<th>Amount</th>
			<th></th>
		</tr>
		</thead>
		<tbody>
		<% @expenses.each_with_index do |expense, i| %>
			<% @expenses_total = expense.amount_raw %>
		<tr>
			<td><%= expense.date  %></td>
			<td><%= link_to expense.expense_type, expense_path(expense.id)  %></td>
			<td><%= expense.description %></td>		
			<td><%= expense.amount %></td>
			<td width="80px">
			<span class="pull-right">
				<%= link_to "Edit", edit_expense_path(expense.id)%>
				|
				<%= link_to "Delete", expense_path(expense.id), method: :delete, data: { confirm: 'Are you sure?' } %>
			</span>
		</td>
		</tr>

		</tbody>
		<%end%>
		<tr>
			<td colspan="7">
				Total Expenditures for Year: <%= @expenses_total %>
				</a>
			</td>
		</tr>
	</table>
</div>

