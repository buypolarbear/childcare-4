/**
 This is executed when the child_arrive_depart#toggle_signin
 controller method is called with the "js"
 format (i.e. AJAX call instead of a redirect).
 */
 
 /**
$("#modal").dialog({
  autoOpen: true,
  height: 600,
  width: 600,
  modal: true,
  title: 'Edit Person',
  buttons: {
    "Edit": function() { $("#edit_user_<%= @child.id %>").submit() },
  },
  open: function() {
    $("#modal").html("<%= escape_javascript(render('modal')) %>")
  },
});
*/

$modal = $('.modal'),
$modalBody = $('.modal .modal-body'),
$modalHeading = $('.modal .modal-heading');
$modalFooter = $('.modal .modal-footer');
$modalHeading.html("Unverified Guardian");
$modalBody.html("Do you want to verify the identity of <%= @parent_guardian.full_name %> and allow them to manage <%= @child.full_name %>?");
$modalFooter.html("<%= escape_javascript(render('partials/modal_verify')) %>");
$modal.modal();

/* Toggles the visibility of the list of parents for the current child */
$( "#parent_list_<%= @child.id %>").fadeToggle( "fast" );
