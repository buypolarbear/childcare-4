<h3>Tap your child's name</h3>

<% @children.each do |child| %>
	
	<!-- CHILD BUTTON -->
	<label id=<%= child.id %>>
		<b><%= link_to child.full_name,  {:controller => "child_arrive_depart", :action => "children", :remote => true, :id => child.id  }, style: "color:white", class: "btn btn-primary btn-block span6" %></b>
	</label>
	<!-- END Child button -->
	
	<!-- Parents button -->
	<div id="parent_list_<%= child.id %>" style="display: none">
			<!-- this will get replaced with the content of the child's parents -->
	</div>
	<!-- END parents button -->
	
	<!-- TODO: add some sort of spacer here to give us some room between this child and the next -->

<%end%>

<div class="modal hide fade">
  <div class="modal-header">
    <button type="button" class="close" data-dismiss="modal" aria-hidden="true">&times;</button>
    <h3 class="modal-heading">Modal header</h3>
  </div>
  <div class="modal-body">
    <p>One fine body…</p>
  </div>
  <div class="modal-footer">	
	<a href="#" class="btn btn-primary">Child is Arriving</a>
	<a href="#" class="btn btn-primary">Child is Departing</a>
    <a href="#" class="btn" data-dismiss="modal">Cancel</a>
  </div>
</div>






