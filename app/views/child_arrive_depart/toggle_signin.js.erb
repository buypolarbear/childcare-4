/**
 This is executed when the child_arrive_depart#toggle_signin
 controller method is called with the "js"
 format (i.e. AJAX call instead of a redirect).
 */

 alert("<%= @parent_guardian.full_name %> created child event with timestamp <%= @event.start_time %> for <%= @child.full_name %>"); /* Just for debugging */
